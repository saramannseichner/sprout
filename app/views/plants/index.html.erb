<div class="plants-container">
  <div class="cards">
    <% @plants.each_slice(2) do |plants| %>
      <div class="card-row">
          <div class="col-xs-12 col-md-6 no-gutter" >
            <%= render partial: 'components/plant', locals: { plant: plants.first, height: "700px", pos: "1" } %>
          </div>
          <div class="col-xs-12 col-md-6 no-gutter" >
            <%= render partial: 'components/plant', locals: { plant: plants.last, height: "700px", pos: "2" } %>
          </div>
        <div class="w-100"></div>
      </div>
    <% end %>
  </div>
</div>


